apiVersion: 1
providers:
  - name: default
    folder: "" # 폴더 없이 General(루트)에 노출 → 홈/대시보드 목록에서 바로 접근
    type: file
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
